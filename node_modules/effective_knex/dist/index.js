const { handleCallbacks, convert, testConvert, getCleanObject, testGetCleanObject } = require("./getCleanObject")
const { getInfo, getAllTables, getTables, getForeignTables } = require("./getInfo")
const { dataExistsAndMatches } = require("./dataExistsAndMatches")
const { cleanPostObject, testCleanObject } = require("./cleanPostObject")
const { postFakeData, getTableBody, makeData } = require("./postFakeData")
const { cleanUpdateObject, testCleanUpdateObject, getCurrentIds, setup } = require("./cleanUpdateObject")
const { getObject } = require("./getObject")
const { tableColValCount, testCount, getObjectCount } = require("./counts.js")
const { delById, delByKeyValue, deleteFromOtherTables, del, testDelete } = require("./delete.js")
const { getOneById, getOneByKeyAndValue, getByKeyValue, getByObject, getAll, testBasic } = require("./get.js")
const { post, update, testPostAndUpdate, updateOne } = require("./basic.js")
const { list } = require("./list")
module.exports = {
    list,
    getObjectCount,
    updateOne,
    getOneByKeyAndValue,
    getOneById,
    getByObject,
    getByKeyValue,
    getAllTables,
    testGetCleanObject,
    testPostAndUpdate,
    delById,
    delByKeyValue,
    deleteFromOtherTables,
    testBasic,
    testDelete,
    tableColValCount,
    testCount,
    testCleanUpdateObject,
    convert,
    testConvert,
    getCurrentIds,
    setup,
    testCleanObject,
    post,
    update,
    getForeignTables,
    getObject,
    getTables,
    getAll,
    del,
    cleanUpdateObject,
    cleanPostObject,
    dataExistsAndMatches,
    getCleanObject,
    getInfo,
    postFakeData,
    getTableBody,
    makeData,
    handleCallbacks
}