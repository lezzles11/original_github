get:
  description: get total seconds for problem
  summary: get all
  parameters:
    - $ref: "#/definitions/parameters/user_id"
    - $ref: "#/definitions/parameters/problem_id"
  tags:
    - hourglass
  responses:
    "200":
      description: seconds for problem (calculated by task)
      schema:
        type: integer
    "400":
      $ref: "#/definitions/error"
