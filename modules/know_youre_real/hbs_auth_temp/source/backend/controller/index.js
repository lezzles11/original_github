class Test {
  /**
   * let test = new Test()
   * @date 2022-02-28
   * @returns {class}
   */
  constructor() {}
  /**
   * test,one(hello)
   * @date 2022-02-28
   * @param {any} two
   * @returns {any}
   */
  one(two) {
    let three = two + two;
    console.log(three);
    return three;
  }
}
